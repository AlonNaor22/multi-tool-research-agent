[pytest]
asyncio_mode = auto

# Fail any single test that takes longer than 30 seconds (likely frozen)
timeout = 30

# -n auto: run tests across all CPU cores via pytest-xdist
# --tb=short: concise tracebacks on failure
addopts = --tb=short -n auto
